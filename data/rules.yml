version: "3.1"
rules:
  - rule: Gemini fallback for out of scope
    steps:
      - intent: out_of_scope
      - action: action_gemini_response

  - rule: Ask the user to rephrase for nlu_fallback
    steps:
      - intent: nlu_fallback
      - action: action_gemini_response
